// export const getMainBranch = (hostel: any, parentHostelId : string) => {
//     let HashMap = new Map<string, string>();

//     // insert the data on the hashmap
//     for (let i = 0; i < hostel.length; i++) {
//         HashMap.set(hostel[i].id, hostel[i].parentHostelId);
//     }

//     // now get the main branch
//     while(HashMap.)
// }